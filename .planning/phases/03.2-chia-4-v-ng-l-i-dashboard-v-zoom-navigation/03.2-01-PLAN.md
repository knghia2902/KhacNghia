---
wave: 1
depends_on: []
autonomous: true
files_modified:
  - "src/pages/Home.jsx"
  - "src/pages/Docs.jsx"
  - "src/components/canvas/IsometricCanvas.jsx"
  - "src/isometric.css"
---

# Plan 01: Build One-Map Isometric Canvas

<objective>
Xây dựng cơ chế lưới Isometric duy nhất (One-Map Canvas) để hợp nhất trang Dashboard (`Home.jsx`) vào `Docs.jsx`. Canvas hỗ trợ Drag/Pan thả tự do, Scroll Wheel thu phóng camera, và định vị ngẫu nhiên các khu vực (`Docs`, `Tools`) bằng mô hình tĩnh 3D.
</objective>

## Tasks

<task>
<read_first>
- src/isometric.css
- .planning/phases/03.2-chia-4-v-ng-l-i-dashboard-v-zoom-navigation/03.2-CONTEXT.md
</read_first>
<action>
Tạo `src/components/canvas/IsometricCanvas.jsx`.
Thiết lập state `pan` ({x, y}) và `zoom`. 
Gắn sự kiện `onPointerDown`, `onPointerMove`, `onPointerUp` (hoặc react-use-gesture) vào div ngoài cùng `viewport`.
Gắn sự kiện `onWheel` để đổi độ phóng đại `zoom`.
Phần lõi bên trong sử dụng class `isometric-world iso-floor` (từ phase cũ) nối với thuộc tính `style={{ transform: "scale(zoom) translate(pan.x, pan.y)" }}`.
</action>
<acceptance_criteria>
- Tệp `src/components/canvas/IsometricCanvas.jsx` tồn tại.
- Code hỗ trợ cập nhật `pan` và `zoom` thông qua events click/wheel.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/components/canvas/IsometricCanvas.jsx
</read_first>
<action>
Code cấu trúc hiển thị danh sách các Zones trên mặt phẳng `.iso-floor`.
Đối với **Docs Zone**: Render thư viện sách `Meshy_AI_The_Lanterned_Archive_0414101610_texture.glb`. Position absolute tại { bottom: 20%, right: 20% }. Đi kèm hiệu ứng click để mở UI.
Đối với **Tools Zone**: Render lò rèn `LoRen.glb`. Position absolute tại { top: 20%, left: 20% }.
</action>
<acceptance_criteria>
- Component chứa `<model-viewer>` load asset `LoRen.glb` tại vị trí top, left.
- Tương tác onClick vào `.zone-pedestal` trả về `zoneId` cụ thể.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/pages/Docs.jsx
- src/pages/Home.jsx
</read_first>
<action>
Khởi tạo canvas làm nền trong `src/pages/Docs.jsx`. Xóa cấu hình 4 grid column trên `Home.jsx` và gộp lại hoặc đặt redirect Home về Docs.
Tất cả UI overlay (Ví dụ: `CascadingNav`, Editor Layout) phải đổi thành `pointer-events: none` hoặc được bao bọc z-index cao để Map click/drag ở background không bị cản trở.
</action>
<acceptance_criteria>
- Component `Home.jsx` đã được dọn sạch lưới 4 vùng grid.
- `Docs.jsx` đã bọc lại giao diện dạng Overlay, bên dưới là `<IsometricCanvas>`.
</acceptance_criteria>
</task>

## Verification
- Mở URL và kiểm tra drag vuốt thả trên web không giật lag.
- Scroll web kích hoạt scale zoom thay vì cuộn trang.
- Trấn pháp `<model-viewer>` của Lò rèn tải 3D trên lưới dot-grid. 
