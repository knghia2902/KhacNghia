---
phase: 2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/commandUtils.js
  - src/hooks/useCommandSearch.js
  - src/App.jsx
  - src/pages/Tools.jsx
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-07
  - UI-05
  - UI-06
---

<objective>
Set up the core utilities, search hook with debouncing, and basic routing/launcher for the Network Command Lookup tool.
</objective>

<tasks>

<task id="02-01-01" type="execute">
<title>Create commandUtils.js for clean copy and styling</title>
<read_first>
- .planning/phases/02-search-ui-command-display/02-PATTERNS.md (for commandUtils.js pattern)
</read_first>
<action>
Create `src/utils/commandUtils.js` with the following content:
1. `cleanCommandSyntax(fullSyntax, promptMode = '')`: Strip prompts like `Router# ` or `[Huawei] ` using regex.
2. `copyToClipboard(text)`: Async clipboard writing with a textarea fallback using `document.execCommand('copy')`.
3. `VENDOR_THEMES` object mapping vendors (cisco, fortinet, juniper, palo_alto, mikrotik, aruba_hpe, huawei) to `{ bg, text, label }`.
4. `getVendorStyle(vendor)`: Helper to return matching theme or default styling based on `vendor.slug`.
</action>
<acceptance_criteria>
- `grep -q "cleanCommandSyntax" src/utils/commandUtils.js`
- `grep -q "copyToClipboard" src/utils/commandUtils.js`
- `grep -q "VENDOR_THEMES" src/utils/commandUtils.js`
</acceptance_criteria>
</task>

<task id="02-01-02" type="execute">
<title>Create useCommandSearch.js hook</title>
<read_first>
- .planning/phases/02-search-ui-command-display/02-PATTERNS.md (for useCommandSearch.js pattern)
</read_first>
<action>
Create `src/hooks/useCommandSearch.js` implementing:
1. Import `useState`, `useEffect`, `useCallback` from React, and `supabase` client, `useAuth` from AuthContext.
2. Initialize state for: `query`, `debouncedQuery` (200ms debounce), filter selections (`selectedVendor`, `selectedDeviceType`, `selectedCategory`, `favoritesOnly`), `commands` results array, `loading`, `error`, `metadata` (vendors, deviceTypes, categories), and `favoriteIds`.
3. `useEffect` to fetch metadata tables on mount (`vendors`, `device_types`, `command_categories`).
4. `useEffect` to trigger Supabase RPC `search_network_commands` when `debouncedQuery` or filters change. Pass `p_query`, `p_vendor_id`, `p_device_type_slug`, `p_category_slug`, `p_limit` (50), and `p_offset` (0).
5. Add `toggleFavorite` logic that interacts with `command_favorites` table.
Return all state and setter functions.
</action>
<acceptance_criteria>
- `grep -q "export function useCommandSearch" src/hooks/useCommandSearch.js`
- `grep -q "supabase.rpc('search_network_commands'" src/hooks/useCommandSearch.js`
- `grep -q "debouncedQuery" src/hooks/useCommandSearch.js`
</acceptance_criteria>
</task>

<task id="02-01-03" type="execute">
<title>Update App Route and Tools Launcher</title>
<read_first>
- src/App.jsx (to insert new route)
- src/pages/Tools.jsx (to insert launcher card)
- .planning/phases/02-search-ui-command-display/02-PATTERNS.md (for routing and launcher patterns)
</read_first>
<action>
1. Edit `src/App.jsx` to import `NetworkCommands` (create a dummy `src/pages/NetworkCommands.jsx` returning `<div />` if it doesn't exist yet to avoid crashes). Add `<Route path="/tools/network-commands" element={<NetworkCommands />} />` inside the main Routes component under `<Layout>`.
2. Edit `src/pages/Tools.jsx`. In the `INITIAL_TOOLS` array, add a new tool object for the Network Command Lookup (id: 'network-commands', title: "Network Command Lookup", desc: "Tra cứu cú pháp lệnh mạng đa hãng...", icon: "terminal", link: "/tools/network-commands").
3. Ensure `handleLaunch` in `src/pages/Tools.jsx` correctly uses `useNavigate` to route internally if `link.startsWith('/')`.
</action>
<acceptance_criteria>
- `grep -q "/tools/network-commands" src/App.jsx`
- `grep -q "Network Command Lookup" src/pages/Tools.jsx`
</acceptance_criteria>
</task>

</tasks>

<verification>
- Run application build to ensure `commandUtils.js` and `useCommandSearch.js` compile successfully.
- Ensure route `/tools/network-commands` exists without errors.
</verification>

<success_criteria>
- `commandUtils.js` exports formatting and clipboard tools.
- `useCommandSearch` correctly manages debouncing and filter state.
- Route and launcher for Network Commands are present.
</success_criteria>

<must_haves>
- `cleanCommandSyntax` logic is implemented.
- The route `/tools/network-commands` is accessible.
- `search_network_commands` RPC is integrated.
</must_haves>

<threat_model>
- Bug: RPC call invoked on every keystroke if debounce is missing.
- Mitigation: Enforce strict 200ms debounce in `useCommandSearch`.
- Bug: App crashes due to missing `NetworkCommands` component in App.jsx.
- Mitigation: Supply placeholder for `NetworkCommands.jsx` during this wave.
</threat_model>
