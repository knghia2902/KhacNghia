# Phase 03.2: chia-4-v-ng-l-i-dashboard-v-zoom-navigation - Validation Strategy

## 1. Unit & Component Verification

### 1.1 Map Drag and Zoom
- **Action**: Test pan (drag) event handlers.
- **Criteria**: State variables `pan` and `zoom` update correctly without performance bottlenecks.

### 1.2 Model Rendering
- **Action**: Ensure `<model-viewer>` loads assets.
- **Criteria**: Models (`LoRen.glb` and `Meshy_AI...`) render with appropriate scale parameters.

## 2. Integration Verification

### 2.1 Zone Interaction
- **Action**: Verify clicking on the 3D zone mesh/pedestal.
- **Criteria**: `activeZone` state updates in Context or Component level, triggering floating UI overlays.

## 3. End-to-End Walkthrough

### 3.1 One-Map User Journey
- **Setup**: Open the App (routing to "/" or "/docs").
- **Execution**: User drags the map to pan, scrolls out to see the overarching view, and clicks on the `Docs` zone.
- **Verification**: The cascaded Docs Panels slide out successfully over the 3D map. Clicking away or zooming out dismisses the panel UI.
