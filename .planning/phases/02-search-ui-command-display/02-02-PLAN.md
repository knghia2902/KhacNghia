---
phase: 2
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - src/components/network-commands/SearchHeader.jsx
  - src/components/network-commands/FilterBar.jsx
  - src/components/network-commands/CommandCard.jsx
  - src/components/network-commands/CommandDrawer.jsx
  - src/components/network-commands/Toast.jsx
  - src/pages/NetworkCommands.jsx
autonomous: true
requirements:
  - SRCH-03
  - SRCH-04
  - SRCH-05
  - SRCH-06
  - UI-01
  - UI-02
  - UI-03
  - UI-04
---

<objective>
Implement the UI components for the Network Command Lookup tool, including the search grid, filter bar, command cards, and detail drawer.
</objective>

<tasks>

<task id="02-02-01" type="execute">
<title>Create SearchHeader and FilterBar components</title>
<read_first>
- .planning/phases/02-search-ui-command-display/02-PATTERNS.md (for component patterns)
</read_first>
<action>
1. Create `src/components/network-commands/SearchHeader.jsx`: Input for `query` state, attach a window `keydown` listener for `Ctrl+K` and `/` to focus the input. Show `totalResults` below the input.
2. Create `src/components/network-commands/FilterBar.jsx`: Accept metadata (vendors, deviceTypes, categories) and state setters. Render chips for vendors and devices, a toggle for favorites, and a dropdown for categories. Provide a "Reset" button when filters are active. Use `getVendorStyle` for vendor chips.
</action>
<acceptance_criteria>
- `grep -q "Ctrl+K" src/components/network-commands/SearchHeader.jsx` || `grep -q "ctrlKey" src/components/network-commands/SearchHeader.jsx`
- `grep -q "FilterBar" src/components/network-commands/FilterBar.jsx`
</acceptance_criteria>
</task>

<task id="02-02-02" type="execute">
<title>Create CommandCard and Toast components</title>
<read_first>
- .planning/phases/02-search-ui-command-display/02-PATTERNS.md (for CommandCard pattern)
</read_first>
<action>
1. Create `src/components/network-commands/CommandCard.jsx`: Displays vendor badge, device tags, title, description, and syntax code block. Shows a red warning badge if `is_destructive` is true. Contains a star icon for favorite and a copy button that calls `onCopy` with the output of `cleanCommandSyntax(command.full_syntax || command.command_syntax, command.prompt_mode)`.
2. Create `src/components/network-commands/Toast.jsx`: A simple fixed notification component that renders conditionally when a copy action is successful.
</action>
<acceptance_criteria>
- `grep -q "cleanCommandSyntax" src/components/network-commands/CommandCard.jsx`
- `grep -q "Toast" src/components/network-commands/Toast.jsx`
</acceptance_criteria>
</task>

<task id="02-02-03" type="execute">
<title>Create CommandDrawer component</title>
<read_first>
- .planning/phases/02-search-ui-command-display/02-PATTERNS.md (for CommandDrawer pattern)
</read_first>
<action>
Create `src/components/network-commands/CommandDrawer.jsx`:
1. Renders via `ReactDOM.createPortal` in a fixed overlay.
2. Includes a backdrop that closes the drawer on click, and an `Escape` key listener.
3. Displays the full command title, vendor tags, warning badge, description, and parameters/examples table from JSONB properties if they exist.
4. Includes a 1-click copy button and favorite toggle.
</action>
<acceptance_criteria>
- `grep -q "ReactDOM.createPortal" src/components/network-commands/CommandDrawer.jsx`
- `grep -q "CommandDrawer" src/components/network-commands/CommandDrawer.jsx`
</acceptance_criteria>
</task>

<task id="02-02-04" type="execute">
<title>Assemble NetworkCommands Page</title>
<read_first>
- .planning/phases/02-search-ui-command-display/02-PATTERNS.md
</read_first>
<action>
Update `src/pages/NetworkCommands.jsx`:
1. Consume `useCommandSearch` hook.
2. Render `SearchHeader` and `FilterBar` passing the appropriate state and setters.
3. Render a responsive grid (e.g., `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`) of `CommandCard` components mapping over the `commands` array.
4. Maintain `selectedCommand` state to control the visibility of `CommandDrawer`.
5. Maintain toast state to show `Toast` component when copying commands.
6. Display a loading skeleton or "No results" empty state when applicable.
</action>
<acceptance_criteria>
- `grep -q "useCommandSearch" src/pages/NetworkCommands.jsx`
- `grep -q "<CommandCard" src/pages/NetworkCommands.jsx`
- `grep -q "<CommandDrawer" src/pages/NetworkCommands.jsx`
</acceptance_criteria>
</task>

</tasks>

<verification>
- Navigate to `/tools/network-commands`. 
- Type `Ctrl+K` and verify the search input focuses.
- Verify that filtering by vendor/device updates the displayed cards.
- Verify clicking a card opens the right drawer, and clicking outside closes it.
</verification>

<success_criteria>
- Fully interactive Network Commands page is assembled and operational.
- Filter chips and search bar correctly narrow down results via RPC hook.
- Detailed drawer cleanly presents JSONB properties (examples, parameters) and handles interactions.
</success_criteria>

<must_haves>
- Command copying cleanly strips prompts.
- Destructive commands show warnings.
- State coordinates flawlessly across Header, Filter, Grid, and Drawer.
</must_haves>

<threat_model>
- Bug: Z-index clashes between the drawer and existing site layouts (like navigation).
- Mitigation: Use `ReactDOM.createPortal` and a very high z-index (e.g., `z-[250]`) for the drawer overlay.
</threat_model>
