# Testing

- Currently, there are **NO** testing frameworks or test configurations initialized in the project (no Vitest, Jest, Cypress, or Playwright).
- There are no tests in the codebase (no `*.test.jsx` or `*.spec.js`).
- Features must be tested manually in the browser.
