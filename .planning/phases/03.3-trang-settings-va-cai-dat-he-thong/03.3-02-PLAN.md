---
phase: 03.3
wave: 2
depends_on: ["03.3-01-PLAN.md"]
files_modified:
  - "src/pages/Docs.jsx"
  - "src/isometric.css"
autonomous: true
requirements: []
---

# Plan 02: World Editor Overlay và Gizmos

<objective>
Tích hợp chế độ Edit Mode (`isEditMode`) vào không gian 3D của `Docs.jsx`. Thêm lưới căn chỉnh (Grid overlay) và xử lý sự kiện kéo-thả (Drag handles) để thay đổi vị trí, kích thước, và góc quay cho các Model 3D.
</objective>

<tasks>

```xml
<task>
  <id>03.3-02-01</id>
  <name>Tạo Edit Mode Overlay và Form nhập liệu</name>
  <read_first>
    - src/pages/Docs.jsx
  </read_first>
  <action>
    - Ensure `isEditMode` state is defined in `Docs.jsx` and toggled from `AdminPanel.jsx`.
    - When `isEditMode === true`, dim the other zones and draw a bright grid (using SVG or CSS dots) inside the Admin zone.
    - Render a floating "World Editor Panel" containing input fields: `X`, `Y`, `Rotation`, `Scale` for the currently selected object.
    - Render 'Lưu Thay Đổi' (Save) and 'Huỷ' (Cancel) buttons.
  </action>
  <acceptance_criteria>
    - `src/pages/Docs.jsx` contains `const [isEditMode, setIsEditMode] = useState(false)`
    - Panel with text `Lưu Thay Đổi` exists when `isEditMode` is true.
  </acceptance_criteria>
</task>

<task>
  <id>03.3-02-02</id>
  <name>Tích hợp Drag & Drop cho Models trong 3D Space</name>
  <read_first>
    - src/pages/Docs.jsx
    - src/isometric.css
  </read_first>
  <action>
    - Add drag event handlers (`onPointerDown`, `onPointerMove`, `onPointerUp`) to the `<model-viewer>` wrappers.
    - Transform pointer movement into isometric coordinate changes (x, y). 
    - Update the local draft state of the model's position when dragging.
    - Reflect changes visually via `transform: translate3d(x, y, 0)` in real time.
  </action>
  <acceptance_criteria>
    - Wrapper around `<model-viewer>` contains `onPointerDown` pointer event handlers.
    - Isometric coordinate translation math is present in Docs.jsx drag logic.
  </acceptance_criteria>
</task>
```

</tasks>

<verification>
### Unit / Integrations
- Command `npm run build` passes.

### Goal Backward
Must Haves:
- Edit Mode dims the screen appropriately.
- Components can be dragged.
</verification>
