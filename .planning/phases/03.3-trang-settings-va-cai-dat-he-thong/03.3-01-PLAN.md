---
phase: 03.3
wave: 1
depends_on: []
files_modified:
  - "src/lib/supabaseClient.js"
  - "src/context/AuthContext.jsx"
  - "src/context/SettingsContext.jsx"
  - "src/components/panels/AdminPanel.jsx"
autonomous: true
requirements: []
---

# Plan 01: Thiết lập Database và Settings Panel Cơ bản

<objective>
Tạo cấu trúc Supabase (cài đặt db structure thông qua SQL file mô phỏng hoặc Docs migration), mở rộng AuthContext hỗ trợ user settings/avatar, và refactor AdminPanel thành UI Settings cơ bản (Profile, Theme, Password).
</objective>

<tasks>

```xml
<task>
  <id>03.3-01-01</id>
  <name>Khởi tạo Cấu trúc Supabase cho Settings và World Config</name>
  <read_first>
    - src/lib/supabaseClient.js
  </read_first>
  <action>
    Create a new file `supabase/migrations/20260416_settings_schema.sql` (or similar schema doc) indicating:
    - Table `user_settings` with columns: `id` (uuid, auth.users), `theme`, `language`
    - Table `world_config` with columns: `id` (uuid), `user_id` (uuid, auth.users), `config` (jsonb)
    - Bucket: `avatars`
  </action>
  <acceptance_criteria>
    - `supabase/migrations/20260416_settings_schema.sql` contains `CREATE TABLE user_settings` and `CREATE TABLE world_config`
  </acceptance_criteria>
</task>

<task>
  <id>03.3-01-02</id>
  <name>Mở rộng Auth Context và tạo Settings Context</name>
  <read_first>
    - src/context/AuthContext.jsx
    - src/context/ThemeContext.jsx
  </read_first>
  <action>
    - In `AuthContext.jsx`, add `updatePassword(newPassword)` and `uploadAvatar(file)` functions hooking to `supabase.auth` and storage bucket.
    - Create `src/context/SettingsContext.jsx` to manage `world_config` load/update and export `<SettingsProvider>`. 
    - Wire them in `main.jsx` or `App.jsx`.
  </action>
  <acceptance_criteria>
    - `src/context/AuthContext.jsx` contains `updatePassword` and `uploadAvatar` functions.
    - `src/context/SettingsContext.jsx` exports `useSettings`.
  </acceptance_criteria>
</task>

<task>
  <id>03.3-01-03</id>
  <name>Tái cấu trúc AdminPanel thành Sidebar Settings</name>
  <read_first>
    - src/components/panels/AdminPanel.jsx
    - src/components/panels/ToolsPanel.jsx
  </read_first>
  <action>
    Update `AdminPanel.jsx` to render a list of settings groups:
    - Profile (Tên, Email, Avatar)
    - Theme (Chuyển chế độ Giao diện)
    - Security (Đổi mật khẩu)
    - Không Gian (Nút: "🔧 Chỉnh sửa không gian" trigger hook để toggle edit mode).
    Use glassmorphic aesthetics from `ToolsPanel.jsx`.
  </action>
  <acceptance_criteria>
    - `src/components/panels/AdminPanel.jsx` contains '🔧 Chỉnh sửa không gian' string.
    - Panel renders Avatar upload button and theme toggle.
  </acceptance_criteria>
</task>
```

</tasks>

<verification>
### Unit / Integrations
- Run `npm run build` to ensure no compile errors from new contexts.

### Goal Backward
Must Haves:
- Valid `SettingsContext` for downstream world editor.
- AdminPanel correctly exposes setting toggles.
</verification>
