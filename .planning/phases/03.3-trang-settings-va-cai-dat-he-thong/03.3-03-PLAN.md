---
phase: 03.3
wave: 3
depends_on: ["03.3-02-PLAN.md"]
files_modified:
  - "src/pages/Docs.jsx"
autonomous: true
requirements: []
---

# Plan 03: Tương tác Agent và Objects trong Admin Zone

<objective>
Biến các đối tượng tĩnh (objects) trong vùng Admin thành "shortcuts" tương tác. Khi người dùng click vào một đồ vật, Agent sẽ chạy đến đó và tự động mở mục Setting tương ứng (ví dụ: click vào Tủ/Gương -> mở Profile, Búa -> Edit Space).
</objective>

<tasks>

```xml
<task>
  <id>03.3-03-01</id>
  <name>Thêm Objects đại diện cho Settings vào Admin Zone</name>
  <read_first>
    - src/pages/Docs.jsx
  </read_first>
  <action>
    - In `Docs.jsx` inside the Admin group, render placeholders or actual `<model-viewer>` objects meant for Profile, Theme, and Edit Space.
    - Give them unique internal `id`s (e.g. `obj-profile`).
  </action>
  <acceptance_criteria>
    - `src/pages/Docs.jsx` contains at least 3 distinct click targets for admin settings in the JSX rendering.
  </acceptance_criteria>
</task>

<task>
  <id>03.3-03-02</id>
  <name>Điều hướng Agent tới tọa độ Object</name>
  <read_first>
    - src/pages/Docs.jsx
  </read_first>
  <action>
    - Expand `handleObjectClick` or the zone clicking handler.
    - If the user clicks an object inside the Admin zone with a setting `id`, invoke the teleport/walk method and set `activeSettingGroup = 'profile'` (or corresponding).
    - Pass this selection state to `AdminPanel.jsx` so it expands the correct settings accordion/menu tab automatically.
  </action>
  <acceptance_criteria>
    - `Docs.jsx` handles state `activeSettingGroup` and passes it to `AdminPanel`.
  </acceptance_criteria>
</task>
```

</tasks>

<verification>
### Unit / Integrations
- Command `npm run build` passes.

### Goal Backward
Must Haves:
- Admins can navigate the settings by clicking 3D world elements.
</verification>
