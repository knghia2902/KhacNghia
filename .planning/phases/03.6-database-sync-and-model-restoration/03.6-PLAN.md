# Phase 03.6: Đồng bộ hóa Database & Khôi phục Models

Dữ liệu mô hình hiện đã được chuyển sang bảng `world_objects` nhưng code hiện tại (sau khi reset) vẫn đang đọc từ bảng `world_config`. Chúng ta cần cập nhật Context và Page chính để sử dụng kiến trúc mới.

## User Review Required

> [!IMPORTANT]
> Việc cập nhật này sẽ thay đổi cách ứng dụng lưu và tải dữ liệu. Các thay đổi thực hiện trước đây trong `world_config` (phiên bản cũ) sẽ được di trú sang bảng mới `world_objects`.

## Proposed Changes

### 1. Settings Context
- [MODIFY] `src/context/SettingsContext.jsx`:
    - Thêm fetch dữ liệu từ `world_objects`.
    - Cung cấp các hàm `updateWorldObject`, `deleteWorldObject`.

### 2. Docs Page
- [MODIFY] `src/pages/Docs.jsx`:
    - Tiêu thụ `worldObjects` từ Context.
    - Cập nhật logic hiển thị Models dựa trên thực thể (entities) thay vì JSON gộp.

## Verification Plan

### Automated Tests
- Kiểm tra dữ liệu trả về từ Supabase trong tab Network.
- Xác nhận các model (Giường, Lò rèn...) xuất hiện trên bản đồ.

### Manual Verification
- Thử di chuyển 1 model và reload trang.
