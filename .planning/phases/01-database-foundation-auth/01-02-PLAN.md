---
phase: 1
plan: 2
type: execute
wave: 1
depends_on: []
files_modified: ["supabase/migrations/002_search_rpc.sql"]
autonomous: true
requirements: ["DB-07"]
---

<objective>
Create the search RPC function (`search_network_commands`) to support hybrid full-text and trigram search.
</objective>

<threat_model>
- Assets at risk: Database performance, command data leak
- Threat actors: Authenticated users issuing heavy queries
- Attack vectors: Denial of service via complex search queries, unauthorized access to data
- Mitigations applied: `SECURITY DEFINER` with explicit access control, `LIMIT p_limit` bounds to prevent expensive queries, RLS on underlying tables ensures data visibility is enforced.
</threat_model>

<tasks>
<task id="1-2-1" type="execute">
<title>Create search RPC SQL file</title>
<read_first>
- .planning/phases/01-database-foundation-auth/01-PATTERNS.md (for the concrete implementation of the RPC)
</read_first>
<action>
Create file `supabase/migrations/002_search_rpc.sql`.

Write the SQL to create the `search_network_commands` function exactly as specified in Section 2.2 of PATTERNS.md.
The function must use:
- `immutable_unaccent(v_clean_query)`
- `plainto_tsquery('simple', v_unaccented_query)`
- JSON aggregations for `vendor`, `device_types`, `canonical_action`
- Relevance scoring formula combining exact prefix boost, full-text `ts_rank_cd`, and trigram `similarity`.

Copy the exact SQL snippet from PATTERNS.md for `CREATE OR REPLACE FUNCTION search_network_commands`.
</action>
<acceptance_criteria>
- `grep -q 'CREATE OR REPLACE FUNCTION search_network_commands' supabase/migrations/002_search_rpc.sql` returns 0
- `grep -q 'relevance_score FLOAT' supabase/migrations/002_search_rpc.sql` returns 0
- `grep -q 'similarity(c.command_syntax, v_clean_query) \* 4.0' supabase/migrations/002_search_rpc.sql` returns 0
- `grep -q 'LANGUAGE plpgsql' supabase/migrations/002_search_rpc.sql` returns 0
</acceptance_criteria>
</task>
</tasks>

<verification>
Run `ls -la supabase/migrations/002_search_rpc.sql` to ensure the file exists.
</verification>

<success_criteria>
- The SQL file for the RPC function is successfully created with correct ranking algorithm.
</success_criteria>

<must_haves>
- Must include the `search_network_commands` function.
- Must combine FTS and Trigram relevance scoring.
</must_haves>
